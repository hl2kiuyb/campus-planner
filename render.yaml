services:
  - type: web
    name: campus-planner
    runtime: docker
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: campus-planner-db
          property: connectionString

databases:
  - name: campus-planner-db
